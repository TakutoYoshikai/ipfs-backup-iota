#!/usr/bin/env node

const {
  saveHashOfBackup,
  getNewestHashOfBackup,
  sendMessage,
  getKeyPair,
} = require("../index");

if (process.argv[2] === "register") {
  saveHashOfBackup(process.argv[4], process.argv[3]);
}

if (process.argv[2] === "get") {
  console.log(getNewestHashOfBackup(process.argv[3]));
}

